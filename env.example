# このファイルを .env にコピーして値を入れる（ローカル確認用）
# 本番は各ホスティングの環境変数設定に登録すること

# 管理ページのURL（CORS許可元）。ローカルなら http://localhost:3000 など
ALLOW_ORIGIN=http://localhost:3000

# プロキシ認証の合言葉（管理ページからは X-Proxy-Key ヘッダーで送る）
PROXY_KEY=change-me-to-a-long-random-string

# LINEへのリクエストのタイムアウト秒
TIMEOUT=30
